#ifndef RANSOMNOTE_H
#define RANSOMNOTE_H

#include <windows.h>

/* Display the ransom note window (blocking — runs its own message loop) */
void ShowRansomNoteWindow(void);

#endif /* RANSOMNOTE_H */